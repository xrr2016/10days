class TabCard {
  constructor (selector, config = {}) {
    this.container = document.querySelector(selector)
    this.config = config
    this.init()
  }
  init () {}
  render () {}
  clickHandler () {}
}